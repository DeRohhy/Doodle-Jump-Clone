#pragma once

#include <SFML/Graphics.hpp>

namespace GameConstants {
    inline constexpr unsigned int SCREEN_WIDTH = 600;
    inline constexpr unsigned int SCREEN_HEIGHT = 800;

    inline constexpr unsigned int CHUNK_HEIGHT = SCREEN_HEIGHT;

    inline constexpr float GRAVITY = 980.f;
}